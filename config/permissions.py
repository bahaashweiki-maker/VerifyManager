"""
config/permissions.py
─────────────────────────────────────────────────────────────────────────────
מרכז הגדרות ההרשאות למנהלים — VerifyManager

שתי רשימות:
────────────
PERMISSIONS   — כל מפתחות ההרשאה שניתן להקצות למנהל.
                מופיעה אוטומטית במסך "ניהול הרשאות" של admin_manager.
                הוספת שורה כאן → ההרשאה ניתנת לסימון.

ADMIN_MODULES — כל כפתורי הפאנל הראשי.
                מופיע אוטומטית בממשק /admin.
                הוספת שורה כאן → כפתור חדש נוצר ומוסתר/מוצג לפי הרשאה.

──────────────────────────────────────────────────────────────────────────────
להוסיף מודול חדש (AI, חנות, תשלומים, CRM …):
    1. הוסף רשומה ל-PERMISSIONS    → ניתן להקצות למנהל.
    2. הוסף רשומה ל-ADMIN_MODULES  → כפתור יופיע בפאנל.
    3. הוסף ניתוב ב-bot.py         → שורה אחת.
    4. הוסף has_permission()        → בכניסה לפיצ'ר.
   אין לגעת בשום קובץ אחר.
──────────────────────────────────────────────────────────────────────────────
"""

from __future__ import annotations

# ─────────────────────────────────────────────────────────────────────────────
# PERMISSIONS — מפתחות ותוויות לממשק ניהול ההרשאות
# ─────────────────────────────────────────────────────────────────────────────
PERMISSIONS: list[dict[str, str]] = [
    # ─── ניהול כללי ──────────────────────────────────────────────────────────
    {"key": "admin",            "label": "👑 גישה לפאנל ניהול"},

    # ─── מודול אימותים ───────────────────────────────────────────────────────
    {"key": "verify.review",    "label": "🪪 סקירת אימותים"},

    # ─── מודול פרסומים ───────────────────────────────────────────────────────
    {"key": "publish.edit",     "label": "📝 עריכת פרסומים"},
    {"key": "publish.approve",  "label": "✅ אישור פרסומים"},

    # ─── שידורים ─────────────────────────────────────────────────────────────
    {"key": "broadcast.send",   "label": "📢 שליחת שידורים"},

    # ─── משתמשים מאומתים ─────────────────────────────────────────────────────
    {"key": "users.view",       "label": "👥 צפייה במשתמשים מאומתים"},

    # ─── סטטיסטיקות ──────────────────────────────────────────────────────────
    {"key": "stats.view",       "label": "📊 צפייה בסטטיסטיקות"},

    # ─── הגדרות ──────────────────────────────────────────────────────────────
    {"key": "settings.edit",    "label": "⚙️ עריכת הגדרות"},

    # ─── מודולים עתידיים — פשוט בטל-הערה כשבונים ────────────────────────────
    # {"key": "ai.manage",        "label": "🤖 ניהול בינה מלאכותית"},
    # {"key": "shop.manage",      "label": "🛒 ניהול חנות"},
    # {"key": "payments.view",    "label": "💳 צפייה בתשלומים"},
    # {"key": "crm.manage",       "label": "📋 ניהול CRM"},
    # {"key": "catalog.edit",     "label": "📦 עריכת קטלוג"},
    # {"key": "channel.manage",   "label": "📡 ניהול ערוצים"},
]


# ─────────────────────────────────────────────────────────────────────────────
# ADMIN_MODULES — כפתורי פאנל הניהול הראשי
#
# כל רשומה:
#   label    — תווית הכפתור (עם אמוג'י)
#   callback — callback_data שיישלח ל-bot.py
#   requires — רשימת מפתחות הרשאה (OR — מספיק אחד מהם)
#              [] = רק סופר-אדמין (כפתור מוסתר מכל מנהל רגיל)
# ─────────────────────────────────────────────────────────────────────────────
ADMIN_MODULES: list[dict] = [
    {
        "label":    "🪪 מערכת אימותים",
        "callback": "ADMIN_VERIFY",
        "requires": ["verify.review"],
    },
    {
        "label":    "📋 ניהול פרסומים",
        "callback": "pub:main",
        "requires": ["publish.edit", "publish.approve"],
    },
    {
        "label":    "📢 שידורים",
        "callback": "ADMIN_BROADCAST",
        "requires": ["broadcast.send"],
    },
    {
        "label":    "👥 משתמשים מאומתים",
        "callback": "VUSERS_LIST",
        "requires": ["users.view"],
    },
    {
        "label":    "📊 סטטיסטיקות",
        "callback": "ADMIN_STATISTICS",
        "requires": ["stats.view"],
    },
    {
        "label":    "⚙️ הגדרות",
        "callback": "ADMIN_SETTINGS",
        "requires": ["settings.edit"],
    },

    # ─── מודולים עתידיים — פשוט בטל-הערה כשבונים ────────────────────────────
    # {
    #     "label":    "🤖 בינה מלאכותית",
    #     "callback": "ADMIN_AI",
    #     "requires": ["ai.manage"],
    # },
    # {
    #     "label":    "🛒 חנות",
    #     "callback": "ADMIN_SHOP",
    #     "requires": ["shop.manage"],
    # },
    # {
    #     "label":    "💳 תשלומים",
    #     "callback": "ADMIN_PAYMENTS",
    #     "requires": ["payments.view"],
    # },
    # {
    #     "label":    "📋 CRM",
    #     "callback": "ADMIN_CRM",
    #     "requires": ["crm.manage"],
    # },
]
